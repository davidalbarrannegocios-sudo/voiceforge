import { DocsClient } from "./DocsClient";

export const metadata = {
  title: "Documentación API — Elite Labs",
  description: "Referencia completa de la API REST de Elite Labs TTS",
};

export default function DocsPage() {
  return <DocsClient />;
}
